services:
  mysql:
    image: mysql:8.0
    container_name: pedidos360_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: sistema_ventas
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - "3308:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./db/db/docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
    networks:
      - pedidos360_network
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$${MYSQL_ROOT_PASSWORD} --silent"]
      interval: 5s
      timeout: 10s
      retries: 30
      start_period: 90s

  db:
    build: ./db/db
    container_name: pedidos360_db
    restart: on-failure
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/sistema_ventas?createDatabaseIfNotExist=true
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=root
      - PEDIDOS360_REPORTES_INTERNAL_KEY=pedidos360-internal
    healthcheck:
      test: ["CMD-SHELL", "bash -ec ': > /dev/tcp/127.0.0.1/8080'"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 60s
    networks:
      - pedidos360_network

  frontend:
    profiles:
      - old-frontend
    build: ./frontend/frontend
    container_name: pedidos360_frontend_legacy
    ports:
      - "8081:8081"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - pedidos360_network

  sistemareportes:
    build: ./sistemareportes
    container_name: pedidos360_reportes
    restart: on-failure
    ports:
      - "8082:8082"
    environment:
      - PEDIDOS360_PRUEBAS_MYSQL_HOST=mysql
      - PEDIDOS360_PRUEBAS_MYSQL_PORT=3306
      - PEDIDOS360_PRUEBAS_API_URL=http://db:8080/api
      - PEDIDOS360_REPORTES_INTERNAL_KEY=pedidos360-internal
    depends_on:
      db:
        condition: service_healthy
    networks:
      - pedidos360_network

  frontend_node:
    build: ./frontend_node
    container_name: pedidos360_frontend
    restart: on-failure
    ports:
      - "8083:80"
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://127.0.0.1/ || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 20s
    networks:
      - pedidos360_network

volumes:
  mysql_data:

networks:
  pedidos360_network:
    driver: bridge
